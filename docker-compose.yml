version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./generated_letters:/app/generated_letters

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
